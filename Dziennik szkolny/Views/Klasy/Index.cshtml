@model IEnumerable<Dziennik_szkolny.Models.Klasa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NazwaKlasy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazwaKlasy)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_klasy">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_klasy">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_klasy">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
